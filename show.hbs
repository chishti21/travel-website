<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        img{
            width: 300px;
            height: 300px;
        }
        .items{
            display: flex;
            flex-direction: column;
            align-items: center;
        }
    </style>
</head>
<body>
    <div class="items">
    <img src="{{search.imageUrl}}">
    <h1>{{search.name}}</h1>
    <p>Days:{{search.days}} , Nights:{{search.nights}}</p>
    <h1>price:{{search.price}}/- per head</h1>
    <form action="/book" method="POST">
    <button type="submit" name="id" value='{{search._id}}'>book now</button>
    </form>
    </div>
    
</body>
</html>